#include<stdio.h>
#include<stdint.h>
#include<time.h>
#include"input.h"
#include "judge.h"
int main()
{
	int rand();
	//コンピュータの時間を変数に代入
	//int t = (int)time(nullptr);
	//int answer = t % 10;
	// 入力した数字を変数 input に代入する
	while(true)
	{
		int input();

		int judge();

		if (rank = 1)
		{
			break;
		}
	}
	return 0;
}