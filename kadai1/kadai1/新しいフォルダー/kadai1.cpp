#include <stdio.h>

#include <stdlib.h>

#include <time.h>
#include "input.h"
#include "judge.h"

int main()

{
    int rand();

    // キーボードで押した数を変数 input に代入する
    while (true)
    {
        int input();
        int judge(int answer, int input);

    }
    return 0;

}